# 2026-08-26

- 优化 Hero 与 About 之间的滚动过渡：新增 `.hero__fade-edge` 底部渐变遮罩 + About 区块向上重叠 18vh（移动端递减），实现视频到个人介绍的柔和渐变衔接；同时在 `useSiteAnimations.js` 增加 `.about__grid` 基于 ScrollTrigger 的缓慢浮现（opacity/y scrub），满足用户“下滑过程缓慢浮现”的需求。
- 验证：本地 dev server + agent-browser 截图确认过渡无硬边界，构建通过。
- 用户反馈上版“重叠导致 About 盖住 Hero 按钮（联系我/查看作品）”。改为：`.hero` 提升 z-index:5 置于内容层最上方；新增整段 Hero 滚动渐隐（opacity→0 + yPercent -6，scrub），About 置于 Hero 下方随滚动浮现，按钮在 Hero 内随淡出不受遮挡。
