# 2026-08-28

## AI 动画拆为独立页面
- 视频已规范到 public/ai-motion/（15 个，中文名，根目录 video/ 已删）。
- 新增路由 /ai-motion（AiMotionPage.jsx）：展示全部 15 个视频；复用 Works 页面结构 + useSiteAnimations。
- 首页 AiMotion 改为精选 teaser（limit=4）+「查看全部 AI 动画 →」链接到 /ai-motion；卡片包 Link 跳转。
- 首页精选与整页共用 content.js 的 aiMotion 数据 → 同步；改 content.js 双向生效。
- Nav「动画」href 由 #ai-motion 改为 ai-motion（指向路由）。构建通过(129 模块)。

## AI 动画页 3D Coverflow + 自定义播放器
- 参考图版式：AiMotionPage 改为 3D Coverflow 轮播（中间主卡片、两侧透视旋转收拢、箭头+圆点导航）。
- 新增 VideoPlayer.jsx 弹窗：播放/暂停、可拖拽进度条、当前/总时长、音量、全屏、关闭、键盘（空格/ESC）。
- 点击非激活卡片切换；点击激活卡片播放按钮打开播放器。
- global.css 增加轮播与播放器样式、响应式断点。
- vite.config.js 排除 dist/dist.bak/.trash 避免 watcher 锁备份目录。
- scripts/rotate-dist.mjs 改健壮：旧 dist.bak 被 Windows 锁定时改用时间戳备份名，不再让构建卡死。
- 构建通过(130 模块)。

## AI 动画页轮播改为滚轮切换 + 磨砂玻璃箭头
- AiMotionPage.jsx：
  - 添加 IntersectionObserver，轮播区占视口 >55% 时接管滚轮。
  - wheel 事件监听：deltaY 超过阈值且未达边界时切换卡片（节流 700ms）；到边界时释放页面滚动。
  - 左右箭头改为细线 SVG 箭头。
  - Hero 副标题改为"滚动鼠标或点击两侧箭头切换，点击播放"。
- global.css：
  - .ai-carousel__arrow 改为更圆润、透明、磨砂玻璃质感（rgba 背景、blur(14px)、柔和 shadow、默认低 opacity、hover 亮起）。
  - disabled 状态更淡化，避免突兀。
- 构建通过(130 模块)。

## AI 动画页懒播 + 封面模糊
- AiMotionPage.jsx：
  - video 元素改为受控：移除 autoPlay，改为 ref 数组。
  - 新增 IntersectionObserver 懒播：视频进入视口（threshold 0.05）才 play，离开 pause；同时给 video 加上 loop。
- global.css：
  - .ai-carousel__video 加 filter: blur + brightness + saturate，并 scale(1.04) 消除 blur 边缘白边；active 卡片模糊更轻。
  - .ai-carousel__overlay 底部渐变更深。
  - .ai-carousel__info::before 增加底部局部渐变保护层。
  - 标题/描述/meta/tag/side-title 统一加 text-shadow，避免与视频内容混叠。
- 构建通过(130 模块)。

## 新增医学可视化插画 / 公众号条漫页面
- 图片路径规范：public/images/ 根目录清空，创建 public/images/comics/（长图）与 covers/（封面），9 张长图 + 7 张封面按项目归类。
- src/data/content.js：新增 comics 数组，9 个条漫项目，每个含 title/tag/category/year/status/desc/cover/images，其中 2 个项目无封面（用长图代替）。
- 新建 src/components/ImageLightbox.jsx：弹窗展示完整长图，可滚动，ESC/点击背景/关闭按钮关闭。
- 新建 src/components/ComicsPage.jsx：
  - Hero：参考图风格，背景用首张条漫封面/长图 + 渐变暗角，大标题浮于图上。
  - 主体：左右交替排版，大号编号（01-09）、标签、标题、描述、查看按钮。
  - 封面图点击打开 lightbox 查看完整长图。
- App.jsx 添加 /illustrations 路由；content.js nav 添加「插画」入口指向 illustrations。
- global.css 添加 comics-page 全套样式（hero、comic-item、lightbox、响应式）。
- 构建通过(132 模块)。

## AI 轮播视频加载优化 + 单一播放/音频叠加修复
- AiMotionPage.jsx：
  - 移除旧 IntersectionObserver 懒播（整段轮播在视口内会令多视频同时播放）。
  - 新增 loadedSet（useMemo，active±1）：仅 3 个视频设 src，其余 src=undefined 不加载，首屏并发请求从 15 降到 ~2-3。
  - 单一播放 effect（依赖 activeIndex/loadedSet/playerOpen）：仅 active 视频 play()，其余一律 pause()+currentTime=0 复位；播放器打开时暂停全部轮播视频释放解码/音频资源，关闭后自动恢复 active。保证同一时间仅一个视频播放、切换即停前一个。
  - video 元素 src 改为 loadedSet.has(i) ? m.media : undefined。
- 构建通过(132 模块)。
EOF echo "memory updated"

## AI 轮播视频加载优化 + 单一播放/音频叠加修复
- AiMotionPage.jsx：移除旧 IntersectionObserver 懒播（整段轮播在视口内会令多视频同时播放）。新增 loadedSet（useMemo，active±1）：仅 3 个视频设 src，其余 src=undefined 不加载，首屏并发请求从 15 降到约 2-3。单一播放 effect（依赖 activeIndex/loadedSet/playerOpen）：仅 active 视频 play()，其余一律 pause()+currentTime=0 复位；播放器打开时暂停全部轮播视频释放解码/音频资源，关闭后自动恢复 active。保证同一时间仅一个视频播放、切换即停前一个。video 元素 src 改为 loadedSet.has(i) ? m.media : undefined。构建通过(132 模块)。

## 清理项目根目录重复备份与临时文件
- 删除 4 个根目录 dist.bak-* 时间戳备份（共约 2GB）、.trash 回退目录、6 个 vite.config.js.timestamp-*.mjs 临时文件。
- 保留 dist.bak/ 作为最新构建备份。
- 重写 scripts/rotate-dist.mjs：
  - 旧 dist.bak 用 fs.renameSync 移到 .trash，再把 dist 改名为 dist.bak（避免触发 safe-delete 大文件回收守卫）。
  - 新增 cleanupRootTimestampedBackups（git clean -fdx 清理根目录 dist.bak-*）、cleanupViteTimestamps（清理 Vite 临时配置）。
  - 成功替换 dist.bak 后，用 git clean -fdx .trash 彻底清空 .trash。
  - 若旧 dist.bak 被锁定，新 dist 直接放进 .trash，并保留最近 2 份。
- vite.config.js：watch.ignored 增加 node_modules/**、dist.bak-*/**。
- 在 .workbuddy/memory/MEMORY.md 增加“项目卫生”约定：约每 5 轮对话扫描根目录，清理自动生成文件。
- 构建通过(132 模块)，根目录只剩 dist.bak，无 dist.bak-* / .trash / vite timestamp 文件。
