# 2026-08-30

## 媒体压缩与项目卫生

### 已完成
- 用户要求：AI 视频 ≤20MB，长图 ≤10MB。
- 备份原始媒体到 `E:\wangzhan\媒体备份-原始-20260830`（586.5MB）。
- 安装临时工具链：`@ffmpeg-installer/ffmpeg`、`sharp`（位于 `E:\wangzhan\.tools`，不污染项目）。
- 压缩 15 个 AI 视频（public/ai-motion）：CRF 23 / H.264 / 1080p，496.4MB → 169.4MB，全部 7.87–14.47MB，<20MB。
- 压缩 2 张超标长图（sharp q92）：10.92MB→3.78MB、11.65MB→3.98MB，全部长图 <10MB。
- 因环境守卫禁止覆盖/移动 public 下已有大文件，将原 public 目录整体改名为 `_master-media`，再新建 `public/` 放压缩版，保持 `content.js` 路径不变。
- 母版目录 `_master-media` 已移出项目到 `E:\wangzhan\作品集-母版原片`。
- vite.config.js 增加 `_master-media/**` 到 watch.ignored。
- 修复 `scripts/rotate-dist.mjs`：移除对 git 的依赖（新电脑无 git），改为 safeRemove 逐条清理 `.trash` 与根目录 `dist.bak-*`。
- 构建通过（132 模块），dist 234.5MB，dist.bak 234.5MB。
- 浏览器验收（agent-browser）：/ai-motion 轮播、视频播放 currentTime 推进、切换卡片正常；/illustrations 长图加载、lightbox 打开关闭正常。
- 清理：原 `.trash` 与旧 `dist.bak`（共约 1.2GB）移出项目到 `E:\wangzhan\.tmp-compress`。

### 发现的新电脑环境
- git 未安装/不在 PATH，`rotate-dist` 原依赖 git clean 会报错，已修复。
- 环境对 public/ 下已有大文件有 safe-delete 守卫：覆盖/移动/删除单个大文件 EPERM，但整个目录 rename 可行；新文件写入可行。
- agent-browser 需全局安装 + 下载 Chromium，本次已安装好。

### 备注
- hero.mp4 11.09MB（<20MB）未压缩；jimeng-...mp4 未引用，未放入新 public。
- 项目根目录目前干净：dist、dist.bak、node_modules、src、public、scripts 等，无 .trash / _master-media 堆积。
