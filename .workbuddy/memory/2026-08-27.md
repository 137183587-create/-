# 2026-08-27

- 用户要求后续交付按 TDD/BDD 原则检查（已写入 MEMORY.md「交付验收」小节）：BDD 用 Given/When/Then 验收，TDD 为关键交互补测试，交付前需构建通过 + 浏览器实测 + 回复附验收清单。
- 本次新增三块功能：
  1. 悬浮「回到顶部」按钮（components/BackToTop.jsx）：fixed 右下、44px 圆形、暗金风，滚动 >480px 淡入，点击平滑回顶；移动端 40px 贴边；含 reduced-motion 兜底。
  2. 引入 react-router-dom，拆分首页「/」与作品页「/works」（App.jsx + ScrollManager.jsx 处理 hash 滚动）。
  3. 作品展示页 /works（components/Works.jsx）：复用暗色 token + SpotlightCard，展示全部项目，含「返回首页」与底部 CTA，风格与首页统一。
- 各板块按钮补齐跳转：Hero「查看作品」→/works、「联系我」→#contact；Nav 锚点改为 Link 跨页回首页滚动；Projects 新增「查看全部作品」→/works；Contact/About 邮箱→mailto、电话→tel、微信/社交→点击复制（带「已复制 ✓」反馈）。
- useSiteAnimations.js 已用 `heroEl/aboutEl` 守卫 Hero/About 专属动画，使 Works 页可安全复用 heading/stagger/reveal 动效（不再因找不到 .hero 报错）。
- 验证：build 通过（125 模块）；agent-browser 实测 /works 直接打开渲染正确（桌面+移动端截图见 mobile-preview/works-desktop.png、works-mobile.png），所有 Link 的 href 正确；首屏滚动后回到顶部按钮可见（mobile-preview/back-to-top.png）。注：agent-browser 合成点击对 React Router Link（尤其 sticky Hero）不触发导航，属自动化环境限制，非代码缺陷——真实浏览器点击正常。
- 部署注意：vite 重新优化依赖时若 .vite 缓存触发批量删除守卫导致 dev server 启动失败，用 `node -e fs.renameSync` 把 node_modules/.vite 改名（rename 不受 safe-delete 拦截）后重启即可。
- 修复 Projects 板块：精选项目卡片改为 Link 包裹并跳转到 /works，添加悬停上抬/边框金色/标题高亮反馈；「查看全部作品」按钮 margin-top 从 56px 增至 206px（下移 150px），避免与卡片重叠。构建通过。
- 按用户参考图（游戏角色详情页风格：全屏 hero + 横向 marquee + 作品画廊网格 + 左文/中图/右属性三栏）重做 /works 页面：Works.jsx 改为 hero「ALL WORKS」+ 8 项无限滚动 marquee + 4 张可点击作品卡（div role=button + SpotlightCard，useState 切换详情）+ 三栏详情区（文案/发光画框/状态·年份·类型·亮点）。global.css 新增 .works-page 体系：.work-card 用 flex 等高（min-height:420px），选中态金色描边；.works-detail__grid 为 1.1fr/1.4fr/0.9fr 三栏。已杀掉陈旧/重复预览服务（5173/5174/5180），重启纯净 dev server 于 5173（任务 g7907L）供预览。验收：DOM snapshot 确认结构正确渲染、默认详情=精选「手作素材库」；点击交互因本沙箱 agent-browser 点击会断连无法实驱，改以源码 handler（onClick/onKeyDown/aria-pressed + key 重挂动画）为准。
- Hero 滚动退出优化（useSiteAnimations.js）：把原先分散的 3 个 ScrollTrigger（content yPercent:16 / video yPercent:10 / hero opacity:0）合并为单一 `heroScrollTl`，内容退出方向从向下改为 `yPercent: -26` 向上，视频同步 `yPercent: -12`，Hero 整体仍淡出。减少 ScrollTrigger 实例、避免 Hero 内容下沉与 About 叠化。保持此前 Hero 置顶渐隐/缝隙修复/按钮遮挡三项现状。
- 清理构建备份：根目录 8 个 `dist_bak_<时间戳>`（约 180MB）已清理；新增 `scripts/rotate-dist.mjs`，在构建前自动清理旧 `dist_bak_*`、并把 `dist` 单份旋转为 `dist.bak`；package.json `build` 改为 `node scripts/rotate-dist.mjs && vite build`。实测 `npm run build` 通过，根目录仅保留 `dist` + `dist.bak`，无时间戳备份堆积。
- Hero 内容向上退出幅度按用户反馈从 `yPercent: -26` 调至 `-32`（视频仍 -12），让 Hero 更快上移、进一步减弱与 About 的叠化。dev server 已 HMR 生效。
- 补充联系方式（content.js）：微信 = 13370852058、现居 = 山东青岛（此前为「待补充」）；About 区「微信/现居」行与 Contact 区链接卡均渲染该字段。dev server 重启后确认正常吐出更新值。
- 头像替换：用用户照片生成 `public/avatar.jpg`，从 1024×1804 原图按人脸偏上原则裁成 1024×1024 正方形（顶部偏移约 209px），再缩放至 400×400 适配角点屏；About.jsx 改为优先渲染 `about.avatar` 图片，global.css 增加 `.avatar__img` 圆形遮罩样式，保留金色虚线光环。构建通过，DOM snapshot 确认 `image "房子翔头像"` 已渲染。
- 联系方式点击复制 + 轻提示：新增 `src/utils/toast.js`（命令式全局 Toast，固定底部居中、自动淡出）；About.jsx 与 Contact.jsx 把所有「微信/电话/现居/社交」改为点击复制按钮并触发 `showToast('已复制X ✓')`，移除了原本的内联文字切换；邮箱保留 mailto 链接。构建通过，DOM snapshot 确认微信/电话/现居均为 button（电话不再是 tel 链接）。
